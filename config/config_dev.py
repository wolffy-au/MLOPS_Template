# Example config_dev.py
# Environment-specific configurations: You might have separate configuration files for different environments such as development, testing, and production. For example, you could have config_dev.py or parameters_dev.yaml for development settings and similarly for other environments.

# DATA_DIR = '/path/to/dev/data'
# DEBUG_MODE = True
